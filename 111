<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <title>Резиновая батарейка</title>

  <style>
    /* Внешний квадрат */
    .wrapper {
      width: 40vw;
      aspect-ratio: 1 / 1;
      border: 3px solid #000;
      padding: 5%;
      box-sizing: border-box;

      display: flex;
      flex-direction: column;
      justify-content: space-around;
      align-items: center;
    }

    /* Батарейка */
    .battery {
      width: 80%;
      height: 18%;
      border: 0.25em solid #000;
      position: relative;
      box-sizing: border-box;
      display: flex;
      align-items: stretch;
    }

    /* Заполнение */
    .battery-level {
      height: 100%;
    }

    /* Контакт */
    .battery-cap {
      position: absolute;
      width: 6%;
      height: 40%;
      background: #000;
      right: -6%;
      top: 30%;
    }

    /* Уровни */
    .low .battery-level {
      width: 33%;
      background: #ED1C24;
    }

    .medium .battery-level {
      width: 66%;
      background: #FFC60B;
    }

    .high .battery-level {
      width: 100%;
      background: #00A055;
    }
  </style>
</head>

<body>

  <div class="wrapper">
    <div class="battery low">
      <div class="battery-level"></div>
      <div class="battery-cap"></div>
    </div>

    <div class="battery medium">
      <div class="battery-level"></div>
      <div class="battery-cap"></div>
    </div>

    <div class="battery high">
      <div class="battery-level"></div>
      <div class="battery-cap"></div>
    </div>
  </div>

</body>
</html>
